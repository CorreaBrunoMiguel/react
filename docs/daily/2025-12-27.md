# Daily Log — 2025-12-27 (W0D0-N1)

## Plan (o que executei hoje)

- [x] Scaffold Vite React em `app/`
- [x] Configurar Tailwind
- [x] Criar estrutura `src/app`, `src/features`, `src/shared`, `src/styles`
- [x] Rodar `pnpm build` e `pnpm lint`

## QA

- build: ok
- lint: ok
- tailwind: ok (classes aplicando no App.jsx)

## Self-review

**O que ficou bom (3):**

- Separei `app/` dos docs/status, deixando o repo limpo.
- Tailwind integrado com prova visual simples no `App.jsx`.
- Build/lint rodando sem ajuste manual.

**O que ficou ruim (3):**

- Quase me perdi em caminho relativo (`cd bootcamp/react`) — vou sempre validar com `pwd`.
- README inicial saiu incompleto; preciso usar o template sempre.
- Daily log veio com cerca de código por descuido.

**Dúvidas (1–3):**

- Se vamos padronizar também Prettier agora ou só quando entrar UI kit (CP1).

**O que eu faria diferente com mais tempo (1–2):**

- Automatizar “handshake” com um script `pnpm verify` na raiz (mais pra frente).

## Próximo passo

- W0D0-N2 (definir tema do app grande)

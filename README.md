# Bootcamp React do Zero + Tailwind

Repo único do bootcamp. O app React roda em `./app`.

## Requisitos

- Node: 24 (ver `.nvmrc`)
- Package manager: pnpm

## Como rodar

```bash
cd app
pnpm install
pnpm dev
```
